# 07 — 消息侧注入通道上线 + 系统提示侧收尾

**What to build:** L1 召回变为会话流里可见的注入行（带插件署名、`recall` 形态、排在用户新消息之前）；只在有新用户消息到达的步骤触发（轮首 + steering 插话）；系统提示侧动态召回上下文撤除、工具指南改为三条件门控（工具开启 && 画像 ∥ 导航 ∥ 本轮召回命中）。

**Blocked by:** 06（预算/超时函数是注入路径的直接输入）

**Status:** ready-for-agent

- [ ] pre-step 监听 prepend 注册、先 next() 再在消息列表头部插入合成消息
- [ ] 合成消息带来源 `{kind:'plugin', plugin:'dsh-memory', form:'recall'}`，内容 `<relevant-memories>` 包裹 + 引导语 + 预算内命中行
- [ ] 仅新用户来源消息触发检索；工具步透传；召回零命中不注入
- [ ] `memory:recall` 系统提示上下文撤除；指南三条件门控（空库/工具关闭不注入）
- [ ] L0 捕获零改动验证（source 过滤天然排除注入消息）
- [ ] 冒烟（fake ctx）：位置/来源/标签/预算/时机/门控断言
- [ ] 真机验证：真实 dsh 会话确认宿主渲染注入行 + 数据目录 L0 JSONL 不含注入内容
