# 05 — 背景上下文按会话现查

**What to build:** 抽取时的背景参考消息改为按会话从 L0 检索库现查（剔除切片自身成员），移除全局内存背景数组——修掉现存跨会话污染与重启丢背景两个缺陷。

**Blocked by:** 02（需要切片会话标识做剔除）

**Status:** ready-for-agent

- [ ] L0 存储新增按会话取最近消息访问器（走会话索引）
- [ ] 抽取时按切片会话现查背景，按消息 id 剔除切片自身
- [ ] 全局背景内存数组移除
- [ ] 冒烟：按会话取数、切片剔除、A 会话内容不进 B 会话背景
