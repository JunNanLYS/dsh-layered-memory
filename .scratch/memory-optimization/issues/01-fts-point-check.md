# 01 — FTS 防御删除点查预判

**What to build:** 重建/重嵌/导入等"全新增记录"路径的写入成本回到线性——upsert 前的防御性 FTS 删除先做主表存在性点查，行不存在即跳过全 FTS 扫描；外部可见行为与现状逐项相同。

**Blocked by:** None — can start immediately.

**Status:** ready-for-agent

- [ ] 新增记录路径不再触发 FTS 全表扫描（存在性点查走主键索引）
- [ ] 合并/更新路径行为不变：旧内容不再可搜、新内容可搜
- [ ] 冒烟：新记录可搜、合并后旧内容不可搜、重建后 FTS 与主表一致（与现状逐项等价）
- [ ] 现有 25 节冒烟全数通过
