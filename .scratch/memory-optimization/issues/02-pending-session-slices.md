# 02 — 未蒸馏缓冲会话切片基础设施

**What to build:** 未蒸馏缓冲条目携带会话标识并持久化；旧格式数据（无会话标识）迁移为 legacy 组一次性蒸馏；提供按会话分组切片的纯函数——为后续按会话触发、闲置兜底、档位切换、背景隔离铺路。

**Blocked by:** None — can start immediately.

**Status:** ready-for-agent

- [ ] pending 持久化 schema 增加会话标识字段，往返保真
- [ ] 旧格式文件加载不丢数据（无字段归 legacy 组）
- [ ] 按会话分组纯函数（组内按时间序、坏记录丢弃）
- [ ] 冒烟：往返 / 迁移 / 分组断言；现有各节保持通过
