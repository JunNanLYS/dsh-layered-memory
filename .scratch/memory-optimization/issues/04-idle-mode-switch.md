# 04 — 闲置兜底 + 档位切换同步

**What to build:** 会话静默 300 秒后未蒸馏切片自动落袋（只清静默会话，活跃会话继续攒）；用户切换会话档位时，旧切片立即按捕获时档位蒸馏——切到关闭档则挂起，切回时按捕获档位落袋。

**Blocked by:** 03（idle 清切片需复用 warmup 感知的触发决策）

**Status:** ready-for-agent

- [ ] idle 扫描纯函数（静默 ≥300s 才清、off 档会话跳过、活跃不动）
- [ ] 档位切换动作表纯函数（切走按捕获档位清 / 切 off 挂起 / 切回清挂起）
- [ ] 全局定时器 tick + 会话档位存储 set() 挂回调
- [ ] 新配置：闲置静默时长默认 300s
- [ ] 冒烟：扫描与动作表断言
